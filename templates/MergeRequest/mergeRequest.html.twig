{% extends "./base.html.twig" %}

{% block body %}

    <div>
        <ul>

            {% for request in requests %}
                {% if request is defined %}
                    <li>{{ request.title }}</li>
                {% endif %}
            {% endfor %}
        </ul>
    </div>
{% endblock %}